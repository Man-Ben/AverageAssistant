<?xml version="1.0" encoding="UTF-8" ?>
<Shell
    x:Class="AverageAssistant.AppShell"
    xmlns="http://schemas.microsoft.com/dotnet/2021/maui"
    xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
    xmlns:local="clr-namespace:AverageAssistant"
    Title="AverageAssistant">


    <FlyoutItem
        Title = "Main Page"
        >
    <ShellContent
        ContentTemplate="{DataTemplate local:MainPage}"
        Route="MainPage" />
    </FlyoutItem>
    <FlyoutItem
        Title = "Class and Average List"
        >
        <ShellContent
        ContentTemplate="{DataTemplate local:ClassAverageListPage}"
        Route="ClassAverageListPage" />
    </FlyoutItem>
</Shell>
